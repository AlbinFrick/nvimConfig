-- local status, taskwiki = pcall(require, "taskwiki")
-- if (not status) then return end
-- local m = require('albinfrick.keymap')

-- Toggle
-- m.nnoremap('<leader>tt', ':VimwikiToggleListItem<CR>')
-- -- Delete
-- m.nnoremap('<leader>td', ':TaskWikiDelete')

-- taskwiki.setup {
--   config = function()
--     vim.g.taskwiki_markup_syntax = 'markdown'
--   end
-- }
